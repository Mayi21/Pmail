{
  "name": "pmail-receiver",
  "version": "1.0.0",
  "description": "Email Worker for PMail system - handles incoming emails",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "postal-mime": "^2.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "wrangler": "^4.80.0"
  }
}